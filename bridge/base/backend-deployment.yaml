#! backend-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: backend
    namespace: fr-app
    labels:
        com.docker.compose.project: fr_app
        com.docker.compose.service: backend
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: fr_app
            com.docker.compose.service: backend
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: fr_app
                com.docker.compose.service: backend
                com.docker.compose.network.default: "true"
        spec:
            containers:
                - name: backend
                  image: fr_app-backend
                  imagePullPolicy: IfNotPresent
                  args:
                    - sh
                    - -lc
                    - npx prisma migrate deploy && npm run start:dev
                  workingDir: /app
                  env:
                    - name: DATABASE_URL
                      value: "postgresql://postgres:postgres@db:5432/multibank?schema=public"
                    - name: JWT_SECRET
                      value: "dev-secret"
                    - name: PORT
                      value: "3000"
                  ports:
                    - name: backend-3000
                      containerPort: 3000
